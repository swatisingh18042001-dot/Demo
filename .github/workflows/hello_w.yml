name: Hello Workflow

on:
  push:

jobs:
  hello-job:
    runs-on: ubuntu-latest

    steps:
      - name: Print Hello Message
        run: echo "Hello World!, This is ${{ vars.FIRST_NAME }} ${{ secrets.LAST_NAME }}"